const path = require('path');
module.exports = {
  'config': path.resolve('config', 'config.js')
}

// CRIADO ESSE COMANDO POIS NO SEQUELIZE-CLI ELE PROCURA UM CONFIG.JSON, que alteramos para CONFIG.JS, por isso precisa apontar onde está a configuração do banco de dados.
